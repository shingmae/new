{
  "name": "deetsauce",
  "homepage": "http://deetsauce.com/",
  "version": "1.0.0",
  "description": "react blog site",
  "main": "index.js",
  "scripts": {
    "install-all": "cd api && npm install && cd ../frontend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "start-api": "cd api && npm start",
    "start-frontend": "cd frontend && npm start",
    "deploy": "npm run install-all && npm run build-frontend && npm start-frontend && npm start-api"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^6.8.4",
    "nodemon": "^2.0.20"
  }
}
